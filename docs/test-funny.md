# Funny 100 QA

- **TC-01 — Page load:** `/funny` returns successfully and the hero heading renders.
- **TC-02 — Navigation:** `Funny 100` appears in the primary sidebar and opens `/funny`.
- **TC-03 — Core interaction:** Copy, Diagram, Image, and Video tabs switch the presentation while retaining the same ranked items.
- **TC-04 — Search and empty state:** A matching query filters results; a nonsense query shows `No laughs found`.
- **TC-05 — Category filter:** Choosing a category shows only items in that category and updates the count.
- **TC-06 — Copy:** Copy joke writes the rank, title, and copy to the clipboard and shows confirmation.
- **TC-07 — Video concept:** Play concept animates the three storyboard beats and temporarily disables replay.
- **TC-08 — Responsive:** Hero, toolbar, tabs, filters, and cards remain usable at 390 px and 1440 px widths.
- **TC-09 — SEO:** The page includes canonical metadata, social art, and ItemList structured data with 100 entries.
- **TC-10 — Error handling:** Clipboard share falls back when Web Share is unavailable; no external data dependency can break the page.
